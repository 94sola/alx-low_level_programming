#include <stdio.h>

/**
 * main
 * Description: Print the alphabet in reverse order in lowercase.
 * Return: Always 0 (success)
 */
int main(void)
{
	int a;

	for (a = 'z'; a >= 'a'; a--)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}

























